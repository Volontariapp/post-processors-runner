import { OutboxPusher, type EventQueueEntity } from '@volontariapp/database';
import type { Logger } from '@volontariapp/logger';
import type { Redis } from 'ioredis';
export declare class EventQueuePusher extends OutboxPusher<EventQueueEntity<string, unknown>> {
    private readonly logger;
    private readonly redis;
    private readonly MAX_LEN;
    constructor(logger: Logger, redis: Redis);
    private formatMessage;
    private createStreamFields;
    private addEntityToPipeline;
    private executePipeline;
    pushElement(entity: EventQueueEntity<string, unknown>): Promise<void>;
    pushBulkElement(entities: EventQueueEntity<string, unknown>[]): Promise<void>;
}
//# sourceMappingURL=event-queue.pusher.d.ts.map
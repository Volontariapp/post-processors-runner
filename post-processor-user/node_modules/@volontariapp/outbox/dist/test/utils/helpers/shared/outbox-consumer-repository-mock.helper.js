import { jest } from '@jest/globals';
import { makeLoggerMock } from './logger-mock.helper.js';
export function makeOutboxConsumerRepositoryMock(modelClass) {
    const queryBuilderMock = {
        setLock: jest.fn().mockReturnThis(),
        setOnLocked: jest.fn().mockReturnThis(),
        where: jest.fn().mockReturnThis(),
        orderBy: jest.fn().mockReturnThis(),
        take: jest.fn().mockReturnThis(),
        getMany: jest.fn().mockResolvedValue([]),
        update: jest.fn().mockReturnThis(),
        set: jest.fn().mockReturnThis(),
        whereInIds: jest.fn().mockReturnThis(),
        returning: jest.fn().mockReturnThis(),
        execute: jest.fn().mockResolvedValue({
            raw: [{ id: 'dummy-id' }],
            generatedMaps: [],
        }),
    };
    const queryRunnerMock = {
        manager: {
            createQueryBuilder: jest.fn().mockReturnValue(queryBuilderMock),
        },
    };
    const mockRepository = {
        metadata: {
            target: modelClass,
            tableName: 'test_table',
        },
        executeInTransaction: jest.fn((work) => work(queryRunnerMock)),
        toEntities: jest.fn((models) => models),
        toEntity: jest.fn((model) => model),
        find: jest.fn(),
        findOne: jest.fn(),
        findOneOrFail: jest.fn(),
        findById: jest.fn(),
        create: jest.fn(),
        createMany: jest.fn(),
        update: jest.fn(),
        delete: jest.fn(),
        repository: {},
        entityClass: {},
        modelClass: {},
        mapper: {},
        logger: makeLoggerMock(),
    };
    return mockRepository;
}
//# sourceMappingURL=outbox-consumer-repository-mock.helper.js.map
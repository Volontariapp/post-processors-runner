import { AbstractDatabaseHealthProvider } from "../database-health.provider.js";
import { InternalServerError } from "@volontariapp/errors";
export class RedisBridgeHealthProvider extends AbstractDatabaseHealthProvider {
    constructor(provider) {
        super('redis', provider);
    }
    async pingDb() {
        if (!this.client.isConnected()) {
            throw new InternalServerError('Redis provider is not connected', 'HEALTH_CHECK_REDIS_NOT_CONNECTED');
        }
        const driver = this.client.getDriver();
        await driver.ping();
    }
}
//# sourceMappingURL=redis-bridge-health-provider.js.map
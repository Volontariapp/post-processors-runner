import { AbstractDatabaseHealthProvider } from "../database-health.provider.js";
import { InternalServerError } from "@volontariapp/errors";
export class Neo4jBridgeHealthProvider extends AbstractDatabaseHealthProvider {
    constructor(provider) {
        super('neo4j', provider);
    }
    async pingDb() {
        if (!this.client.isConnected()) {
            throw new InternalServerError('Neo4j provider is not connected', 'HEALTH_CHECK_NEO4J_NOT_CONNECTED');
        }
        const driver = this.client.getDriver();
        await driver.verifyConnectivity();
    }
}
//# sourceMappingURL=neo4j-bridge-health-provider.js.map
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
import { dirname, resolve } from 'node:path';
import { fileURLToPath } from 'node:url';
import { Type } from 'class-transformer';
import { IsDefined, ValidateNested } from 'class-validator';
import { BaseConfig, Neo4jConfig, PostgresConfig, RedisConfig, loadConfig, } from '@volontariapp/config';
class IntegrationConfig extends BaseConfig {
    postgres;
    neo4j;
    redis;
}
__decorate([
    IsDefined(),
    ValidateNested(),
    Type(() => PostgresConfig),
    __metadata("design:type", PostgresConfig)
], IntegrationConfig.prototype, "postgres", void 0);
__decorate([
    IsDefined(),
    ValidateNested(),
    Type(() => Neo4jConfig),
    __metadata("design:type", Neo4jConfig)
], IntegrationConfig.prototype, "neo4j", void 0);
__decorate([
    IsDefined(),
    ValidateNested(),
    Type(() => RedisConfig),
    __metadata("design:type", RedisConfig)
], IntegrationConfig.prototype, "redis", void 0);
const currentDir = dirname(fileURLToPath(import.meta.url));
const configDir = resolve(currentDir, '../config');
const config = loadConfig(configDir, IntegrationConfig);
export const postgresConfig = {
    host: config.postgres.host,
    port: config.postgres.port,
    username: config.postgres.username,
    password: config.postgres.password,
    database: config.postgres.database,
    synchronize: true,
    ssl: config.postgres.ssl,
};
export const neo4jConfig = {
    url: `${config.neo4j.scheme}://${config.neo4j.host}:${String(config.neo4j.port)}`,
    authToken: {
        scheme: config.neo4j.scheme,
        principal: config.neo4j.username,
        credentials: config.neo4j.password,
    },
};
export const redisConfig = {
    host: config.redis.host,
    port: config.redis.port,
    password: config.redis.password,
    db: config.redis.dbIndex,
    keyPrefix: config.redis.keyPrefix,
};
//# sourceMappingURL=config.example.js.map
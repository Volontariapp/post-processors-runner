import type { Redis } from 'ioredis';
import type { OutboxEntity } from '../../../outbox/index.js';
import { OutboxPusher } from '../../../outbox/index.js';
export declare class RealRedisPusher<T extends OutboxEntity = OutboxEntity> extends OutboxPusher<T> {
    private readonly redis;
    constructor(redis: Redis);
    pushElement(entity: T): Promise<void>;
    pushBulkElement(entities: T[]): Promise<void>;
}
//# sourceMappingURL=extended-outbox-pusher.helper.d.ts.map
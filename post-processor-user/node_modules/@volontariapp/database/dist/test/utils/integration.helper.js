import { beforeAll, afterAll, beforeEach } from '@jest/globals';
import { testDataSource, initializeTestDb, closeTestDb } from '../data-source.js';
export const setupIntegrationTest = (entitiesToClear = []) => {
    beforeAll(async () => {
        await initializeTestDb();
    });
    afterAll(async () => {
        await closeTestDb();
    });
    beforeEach(async () => {
        for (const entity of entitiesToClear) {
            await testDataSource.getRepository(entity).createQueryBuilder().delete().execute();
        }
    });
};
//# sourceMappingURL=integration.helper.js.map
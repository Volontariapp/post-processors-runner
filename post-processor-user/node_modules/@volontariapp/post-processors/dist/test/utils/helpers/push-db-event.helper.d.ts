import type { Repository } from 'typeorm';
import type { Redis } from 'ioredis';
import type { EventQueueModel } from '@volontariapp/database';
import type { Logger } from '@volontariapp/logger';
import type { ServiceType } from '@volontariapp/shared';
export declare function pushDbEvent(repository: Repository<EventQueueModel>, redis: Redis, logger: Logger, id: string, type: string, targetServices: ServiceType[]): Promise<void>;
export declare function pushDbEvents(repository: Repository<EventQueueModel>, redis: Redis, logger: Logger, events: {
    id: string;
    type: string;
}[], targetServices: ServiceType[]): Promise<void>;
export declare function startOutboxLoop(repository: Repository<EventQueueModel>, redis: Redis, logger: Logger, intervalMs?: number): NodeJS.Timeout;
//# sourceMappingURL=push-db-event.helper.d.ts.map
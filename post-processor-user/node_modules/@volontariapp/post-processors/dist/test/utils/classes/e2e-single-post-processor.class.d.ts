import { SinglePostProcessor } from '../../../core/processors/single.post-processor.js';
import type { StreamEvent, IEventPayload } from '@volontariapp/messaging';
import { EventMessagingType } from '@volontariapp/messaging';
export declare class E2ESinglePostProcessor extends SinglePostProcessor<typeof EventMessagingType.EVENT_CHANGED> {
    processedEvents: {
        event: StreamEvent<IEventPayload>;
        messageId: string;
    }[];
    processError: Error | null;
    failEventIds: Set<string>;
    getCircuitBreaker(): import("../../../index.js").CircuitBreaker;
    protected processEvent(event: StreamEvent<IEventPayload>, messageId: string): Promise<void>;
    shouldProcess(eventType: string): boolean;
    acknowledge(messageId: string): Promise<void>;
}
//# sourceMappingURL=e2e-single-post-processor.class.d.ts.map